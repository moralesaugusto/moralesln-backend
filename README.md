# moralesln-backend

moralesln backend

Internal backend configuration for moralesln.com.

This repository contains environment configuration and basic deployment setup used by internal services.

Setup

docker-compose up -d

Configuration

Environment variables are stored in:

.env
configs/mail.env

Some configuration values were temporarily added during deployment debugging and should be rotated during maintenance.
:::


---- With effort :) --------

